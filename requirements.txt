# VulnMind runtime dependencies.
# Install with:  pip install -r requirements.txt
# Or:            pip install -e .   (preferred during development)

click>=8.0         # CLI framework
rich>=13.0         # Terminal formatting, tables, progress bars, panels
requests>=2.28     # HTTP client for NVD live lookups + Groq AI enrichment
reportlab>=4.0     # PDF report generation
